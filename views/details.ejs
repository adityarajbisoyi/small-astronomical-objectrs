<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comet Details</title>
    <link rel="stylesheet" href="style.css">
</head>
<body class="det">
    <% for(let key in data) {%>
        <%if(data.hasOwnProperty(key)){%>
            <div class="subDet"><%= key %> : <%= data[key] %></div>
        <%}%>
    <%}%>
</body>
</html>